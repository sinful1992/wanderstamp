{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the Holiday Map shell — only opening external links.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
