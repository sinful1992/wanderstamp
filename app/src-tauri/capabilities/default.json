{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the Wanderstamp shell — only opening external links.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
