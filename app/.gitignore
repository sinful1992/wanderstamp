# Generated by `tauri android init` / build — never committed
src-tauri/gen/
src-tauri/target/

# Signing key — keep OFF git; back it up separately
*.keystore
keystore.properties
.keystore-password.txt
